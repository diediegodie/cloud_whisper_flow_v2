
## Linux/global-hotkey

Global hotkey registration may require the  Python package or OS-level accessibility permissions on some Linux setups (especially Wayland). The application falls back to an application-focused F8 QShortcut when a global hotkey cannot be registered.
